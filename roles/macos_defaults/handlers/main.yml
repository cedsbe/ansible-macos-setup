---
- name: Restart Finder
  ansible.builtin.command:
    cmd: killall Finder
  changed_when: false
  failed_when: false

- name: Restart SystemUIServer
  ansible.builtin.command:
    cmd: killall SystemUIServer
  changed_when: false
  failed_when: false
